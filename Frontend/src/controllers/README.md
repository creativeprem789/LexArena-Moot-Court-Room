# Controllers
This folder contains the business logic and state management controllers, often structured as custom React Hooks (e.g., `useAuth.js`, `useData.js`). They bridge the Views and the Models.
