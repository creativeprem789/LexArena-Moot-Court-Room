# Views (Pages)
This folder contains the main page-level components that correspond to your application's routes (e.g., `Home.jsx`, `Dashboard.jsx`, `Login.jsx`). They use components and call controllers to render data.
